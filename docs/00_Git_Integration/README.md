# 🌿 Git ইন্টিগ্রেশন - শুরু থেকে শেষ পর্যন্ত (সম্পূর্ণ গাইড)

> **উদ্দেশ্য**: যারা Git সম্পর্কে কিছুই জানেন না, তাদের জন্য একদম সহজ ভাষায় এবং ধাপে ধাপে সম্পূর্ণ গাইড।

---

## 📋 বিষয়সূচী (Table of Contents)

1. [Git কী এবং কেন দরকার?](#১-git-কী-এবং-কেন-দরকার)
2. [Git ইনস্টলেশন](#২-git-ইনস্টলেশন)
3. [Git প্রথমবার সেটআপ](#৩-git-প্রথমবার-সেটআপ)
4. [আপনার প্রজেক্টে Git শুরু করা](#৪-আপনার-প্রজেক্টে-git-শুরু-করা)
5. [GitHub এ আপলোড করা](#৫-github-এ-আপলোড-করা)
6. [দৈনিক কাজের কমান্ড](#৬-দৈনিক-কাজের-কমান্ড)
7. [সমস্যা সমাধান](#৭-সমস্যা-সমাধান)

---

## ১. Git কী এবং কেন দরকার?

### 🤔 Git কী?
**Git** হলো একটি **Version Control System (VCS)**। সহজ ভাষায়:
- আপনার কোডের **ব্যাকআপ** রাখে
- আপনার কোডের **ইতিহাস** সংরক্ষণ করে (কে, কখন, কী পরিবর্তন করেছে)
- **টিমওয়ার্ক** করতে সাহায্য করে
- **ভুল হলে পুরনো ভার্সনে ফিরে যাওয়া** যায়

### 🎯 কেন দরকার?
| সমস্যা | Git এর সমাধান |
|--------|----------------|
| কোড হারিয়ে গেছে | GitHub/GitLab এ সব সেভ থাকে |
| কে কোড চেঞ্জ করেছে? | সব ইতিহাস দেখা যায় |
| পুরনো ভার্সন চাই | যেকোনো সময়ে ফিরে যাওয়া যায় |
| টিমে কাজ করতে হবে | সবাই একসাথে কাজ করতে পারে |

---

## ২. Git ইনস্টলেশন

### ✅ Windows এ ইনস্টল করা

#### ধাপ ১: ডাউনলোড করুন
1. এই লিংকে যান: [https://git-scm.com/download/win](https://git-scm.com/download/win)
2. **"64-bit Git for Windows Setup"** ডাউনলোড করুন

#### ধাপ ২: ইনস্টল করুন
1. ডাউনলোড করা ফাইলটি ওপেন করুন
2. **"Next"** বাটনে ক্লিক করতে থাকুন (সব ডিফল্ট সেটিংস ঠিক আছে)
3. শেষে **"Finish"** ক্লিক করুন

#### ধাপ ৩: যাচাই করুন
1. **PowerShell** বা **CMD** ওপেন করুন
2. এই কমান্ড লিখুন:
```bash
git --version
```
3. যদি দেখায় `git version 2.43.0` (বা এরকম কিছু), মানে ঠিকমতো ইনস্টল হয়েছে! ✅

---

## ৩. Git প্রথমবার সেটআপ

### 🔧 আপনার নাম এবং ইমেইল সেট করুন

**কেন দরকার?** প্রতিটি কমিটে (মানে প্রতিটি সেভে) আপনার নাম এবং ইমেইল থাকে।

#### কমান্ড:
```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

#### উদাহরণ:
```bash
git config --global user.name "Rahim Ahmed"
git config --global user.email "rahim@gmail.com"
```

### ✅ যাচাই করুন:
```bash
git config --list
```

এটি সব সেটিংস দেখাবে। দেখুন আপনার নাম এবং ইমেইল আছে কিনা।

---

## ৪. আপনার প্রজেক্টে Git শুরু করা

### 📁 ধাপ ১: প্রজেক্ট ফোল্ডারে যান

PowerShell/CMD তে আপনার প্রজেক্ট ফোল্ডারে যান:
```bash
cd "g:\laragon\www\Antigravity\devamanamart-ecommerce"
```

### 🌱 ধাপ ২: Git শুরু করুন

```bash
git init
```

**কী হলো?** এই কমান্ড একটি লুকানো `.git` ফোল্ডার তৈরি করবে। এখানেই সব ইতিহাস থাকে।

### 📝 ধাপ ৩: ফাইল Add করুন

```bash
git add .
```

**মানে কী?** সব ফাইল "staging area" তে যুক্ত হলো (মানে সেভ করার জন্য প্রস্তুত)।

### 💾 ধাপ ৪: প্রথম Commit করুন

```bash
git commit -m "Initial commit - AmanaMart e-commerce project setup"
```

**মানে কী?** এখন সব ফাইল সেভ হয়ে গেলো। `-m` এর পরের অংশ হলো একটা message (বার্তা) যা বলে এই commit এ কী করা হয়েছে।

---

## ৫. GitHub এ আপলোড করা

### 🌐 ধাপ ১: GitHub Repository তৈরি করুন

1. [GitHub.com](https://github.com) এ যান
2. লগইন করুন (বা নতুন অ্যাকাউন্ট তৈরি করুন)
3. উপরে ডানদিকে **"+"** আইকনে ক্লিক করুন
4. **"New repository"** সিলেক্ট করুন

### 📋 ধাপ ২: Repository সেটিংস

- **Repository name**: `amanamart-ecommerce`
- **Description**: "Professional e-commerce platform like Shein"
- **Privacy**: 
  - ✅ **Private** (যদি শুধু আপনি দেখতে চান)
  - ☐ **Public** (যদি সবাই দেখতে পারবে)
- **"Create repository"** বাটনে ক্লিক করুন

### 🔗 ধাপ ৩: Local Git কে GitHub এর সাথে সংযুক্ত করুন

GitHub এ নতুন repository তৈরি হওয়ার পর একটি URL পাবেন। যেমন:
```
https://github.com/your-username/amanamart-ecommerce.git
```

এই কমান্ডগুলো দিন:

```bash
# Main ব্রাঞ্চ নাম দিন
git branch -M main

# GitHub এর সাথে সংযুক্ত করুন
git remote add origin https://github.com/your-username/amanamart-ecommerce.git

# আপলোড করুন
git push -u origin main
```

### 🔐 ধাপ ৪: Authentication (যদি চাওয়া হয়)

#### Option A: Personal Access Token (সবচেয়ে সহজ)

1. GitHub.com এ যান → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. **"Generate new token"** ক্লিক করুন
3. একটি নাম দিন (যেমন: "Laptop Access")
4. **Scopes** এ শুধু **"repo"** টিক দিন
5. নিচে **"Generate token"** ক্লিক করুন
6. Token টি **কপি করে নিরাপদ জায়গায় রাখুন** (এটি আর দেখা যাবে না!)

এবার `git push` করলে:
- **Username**: আপনার GitHub username
- **Password**: উপরের token টি পেস্ট করুন

#### Option B: SSH Key (Advanced - পরে শিখবেন)

---

## ৬. দৈনিক কাজের কমান্ড

### 🔄 প্রতিদিন কাজের ফ্লো

```bash
# ১. কোড লিখুন বা পরিবর্তন করুন...

# ২. কী পরিবর্তন হয়েছে দেখুন
git status

# ৩. সব পরিবর্তন Add করুন
git add .

# ৪. Commit করুন (একটি বার্তা সহ)
git commit -m "Added new product card component"

# ৫. GitHub এ পাঠান
git push
```

### 📊 গুরুত্বপূর্ণ কমান্ড সমূহ

| কমান্ড | কাজ |
|--------|-----|
| `git status` | কোন ফাইল পরিবর্তন হয়েছে দেখুন |
| `git add .` | সব পরিবর্তন যোগ করুন |
| `git add filename.js` | একটি নির্দিষ্ট ফাইল যোগ করুন |
| `git commit -m "message"` | পরিবর্তনগুলো সেভ করুন |
| `git push` | GitHub এ আপলোড করুন |
| `git pull` | GitHub থেকে নতুন পরিবর্তন নামান |
| `git log` | সব commit এর ইতিহাস দেখুন |
| `git log --oneline` | সংক্ষিপ্ত ইতিহাস |

### 💡 ভালো Commit Message লেখার নিয়ম

```bash
# ❌ খারাপ
git commit -m "changes"
git commit -m "fix"
git commit -m "update"

# ✅ ভালো
git commit -m "Added shopping cart functionality"
git commit -m "Fixed login button responsiveness"
git commit -m "Updated PostgreSQL to version 16"
```

---

## ৭. সমস্যা সমাধান

### ❗ সমস্যা ১: "Permission denied" বা Authentication Error

**সমাধান**: Personal Access Token ব্যবহার করুন (উপরে দেখুন)

---

### ❗ সমস্যা ২: "fatal: not a git repository"

**কারণ**: আপনি git init করেননি

**সমাধান**:
```bash
git init
```

---

### ❗ সমস্যা ৩: "Updates were rejected"

**কারণ**: GitHub এ নতুন কোড আছে যা আপনার local এ নেই

**সমাধান**:
```bash
# প্রথমে pull করুন
git pull origin main

# তারপর push করুন
git push
```

---

### ❗ সমস্যা ৪: ভুলে `.env` বা গোপন ফাইল commit করে ফেলেছি

**সমাধান**:
```bash
# ১. .gitignore এ যোগ করুন
echo ".env" >> .gitignore

# ২. Git tracking থেকে সরান
git rm --cached .env

# ৩. Commit করুন
git commit -m "Removed .env from tracking"
git push
```

---

## 🎓 পরবর্তী ধাপ (Advanced Topics)

একবার এই basics আয়ত্ত করার পর:
1. **Branching** শিখুন (নতুন feature নিরাপদভাবে তৈরি করতে)
2. **Merge** শিখুন (দুটি branch একসাথে মেশাতে)
3. **Pull Requests** শিখুন (টিমওয়ার্কের জন্য)
4. **Git GUI Tools** ব্যবহার করুন (যেমন: GitHub Desktop, GitKraken)

---

## 📚 সংক্ষিপ্ত চিট শিট

```bash
# সেটআপ (একবারই করতে হবে)
git config --global user.name "Your Name"
git config --global user.email "your@email.com"
git init
git remote add origin <URL>

# দৈনিক কাজ
git status                          # অবস্থা দেখুন
git add .                           # সব যোগ করুন
git commit -m "Your message"        # সেভ করুন
git push                            # আপলোড করুন
git pull                            # ডাউনলোড করুন

# তথ্য দেখা
git log                             # ইতিহাস
git diff                            # পরিবর্তন দেখুন
git branch                          # ব্রাঞ্চ দেখুন
```

---

## 🔗 সহায়ক লিংক

- [Git অফিসিয়াল ডকুমেন্টেশন](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Learn Git Branching (ইন্টারেক্টিভ)](https://learngitbranching.js.org/)

---

## ✅ চেকলিস্ট

- [ ] Git ইনস্টল করেছি
- [ ] নাম এবং ইমেইল কনফিগার করেছি
- [ ] প্রজেক্টে `git init` করেছি
- [ ] প্রথম commit করেছি
- [ ] GitHub repository তৈরি করেছি
- [ ] GitHub এ push করেছি
- [ ] দৈনিক কমান্ড বুঝেছি

---

**মনে রাখবেন**: Git শেখা একটু সময় নেয়। প্রথম দিকে কঠিন মনে হবে, কিন্তু ধীরে ধীরে অভ্যস্ত হয়ে যাবেন। প্রতিদিন একটু একটু করে প্র্যাকটিস করুন! 🚀

---

*তৈরি করেছেন: Antigravity Team ❤️*
