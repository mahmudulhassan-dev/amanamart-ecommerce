# 🛠️ সেটআপ এবং ইনস্টলেশন গাইড (আপডেটেড)

এই প্রজেক্টটি এখন **ল্যাটেস্ট টেকনোলজি** (Java 21, Spring Boot 3.4, React 18, Vite 6) দিয়ে তৈরি।

## ১. প্রয়োজনীয় টুলস (Prerequisites)

*   **Java JDK 21**: ল্যাটেস্ট ফিচার ব্যবহারের জন্য।
    *   [ডাউনলোড করুন (JDK 21)](https://adoptium.net/)
*   **Node.js (v18 বা v20+)**: Vite 6 এর জন্য রিকমেন্ডেড।
*   **MySQL Database**: লারাগনে বা আলাদাভাবে ইনস্টল থাকতে হবে।

---

## ২. ব্যাকএন্ড রান করার নিয়ম (Spring Boot API)

১. টার্মিনালে প্রজেক্ট রুটে যান: 
   `g:\laragon\www\Antigravity\devamanamart-ecommerce`
   
২. কমান্ড দিন:
   ```bash
   ./mvnw spring-boot:run
   ```
   *সার্ভার পোর্ট: `8080`*

---

## ৩. ফ্রন্টএন্ড রান করার নিয়ম (React + Vite)

১. ফ্রন্টএন্ড ফোল্ডারে যান:
   ```bash
   cd frontend
   ```
   
২. সার্ভার চালু করুন:
   ```bash
   npm run dev
   ```
   *সার্ভার পোর্ট: `5173` (সাধারণত)*

---

## ৪. কানেকশন চেক
ব্যাকএন্ড এবং ফ্রন্টএন্ড দুইটা টার্মিনালে আলাদাভাবে রান করতে হবে।
ব্রাউজারে `http://localhost:5173` ওপেন করলে আপনি সাইটটি দেখতে পাবেন।
