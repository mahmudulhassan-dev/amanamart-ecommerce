# 🎨 ফ্রন্টএন্ড টেকনিক্যাল ডকুমেন্টেশন (React Deep Dive)

এই নোটে ফ্রন্টএন্ডের গুরুত্বপূর্ণ কনফিগারেশন এবং লজিকগুলো ব্যাখ্যা করা হলো।

## ১. প্রজেক্ট সেটআপ (Vite vs CRA)

আমরা **Vite** ব্যবহার করেছি কারণ এটি অনেক দ্রুত।
*   **Vite**: কোড পরিবর্তন করার সাথে সাথে ব্রাউজারে আপডেট হয় (Instant HMR)।
*   **React 18**: লেটেস্ট রিয়েক্ট ভার্সন।

---

## ২. টেলউইন্ড সিএসএস (Tailwind Configuration)

আমরা ম্যানুয়ালি `tailwind.config.js` কনফিগার করেছি যেন এটি আমাদের রিয়েক্ট ফাইলগুলো স্ক্যান করতে পারে।

### কনফিগারেশন ব্যাখ্যা:
```javascript
content: [
  "./index.html",
  "./src/**/*.{js,ts,jsx,tsx}", // src ফোল্ডারের ভেতরের সব ফাইলে ডিজাইন খুঁজবে
],
theme: {
  extend: {
    colors: {
      primary: "#FE4444", // আমাদের কাস্টম 'Shein Red' কালার
    }
  }
}
```
> **সুবিধা**: এর ফলে আমরা `bg-primary` লিখলেই আমাদের সেট করা লাল রংটি পেয়ে যাচ্ছি। বারবার কালার কোড লিখতে হচ্ছে না।

---

## ৩. API ইন্টিগ্রেশন (`ProductGrid.jsx`)

ব্যাকএন্ড থেকে ডাটা আনার জন্য আমরা `useEffect` হুক ব্যবহার করেছি।

### কোড লজিক:
```javascript
useEffect(() => {
    // ১. ব্যাকএন্ডের লিংকে রিকোয়েস্ট পাঠানো
    fetch('http://localhost:8080/api/products')
        // ২. ডাটা JSON আকারে রিসিভ করা
        .then(res => res.json())
        // ৩. স্টেটে (State) ডাটা সেট করা
        .then(data => {
            setProducts(data);
            setLoading(false); // লোডিং বন্ধ করা
        })
}, []);
```
*   **Fetch**: এটি ব্রাউজারের ডিফল্ট ফাংশন যা সার্ভার থেকে ডাটা আনতে পারে।
*   **State**: ডাটা আসার পর `setProducts(data)` কল করলে অটোমেটিক স্ক্রিনে প্রোডাক্টগুলো ভেসে ওঠে।
